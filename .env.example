# Bitget MCP server credentials (optional — market data works without them).
# Create an API key at https://www.bitget.com/en/api-management
# Copy this file to .env (gitignored) or export these in your shell.
# NEVER commit real keys.
BITGET_API_KEY=
BITGET_SECRET_KEY=
BITGET_PASSPHRASE=
